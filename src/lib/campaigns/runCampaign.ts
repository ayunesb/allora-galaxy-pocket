export function runCampaign(): string {
  // In a real scenario this would trigger backend + webhook
  console.log("Campaign is being executed...");
  return "🚀 Campaign executed and alerts triggered!";
}
