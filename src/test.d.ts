
import "@testing-library/jest-dom/matchers";
import "@testing-library/jest-dom";

// Extend the global Window interface to include jest-dom custom matchers
interface Window {
  // Add any custom window properties if needed
}
